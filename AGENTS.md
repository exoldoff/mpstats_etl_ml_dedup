# AGENTS.md

Работай по-русски, структурно и по делу.

## Перед началом

1. Сначала прочитай этот файл.
2. Затем прочитай `docs/AI_INDEX.md`, если он есть.
3. Для нетривиальных задач с выбором зоны проекта, документации или нескольких возможных треков прочитай `docs/AGENT_NAVIGATION.md`.
4. Затем прочитай `docs/ARCHITECTURE.md`, если он есть: это важный контекст для архитектурных решений и исследовательского направления.
5. Для общего контекста используй `README.md` и релевантные разделы `docs/AI_INDEX.md`; отдельный `context.md` больше не ведётся.
6. Если задача зависит от актуального API библиотек или сервисов, используй Context7 MCP. Если Context7 недоступен, используй официальную документацию.
7. Не сканируй весь проект без причины: сначала определи релевантные файлы по задаче.

## Коммиты

- Делай коммиты после законченного изменения.
- Не делай push, если пользователь прямо не попросил.
- В грязном worktree не трогай чужие изменения. Стадь и коммить только свои файлы.

## Архитектурные правила

- Транспорт, UI и GUI должны быть тонкими.
- Бизнес-логика должна жить в сервисах.
- Работа с файлами, SQL и другими хранилищами должна быть вынесена в repositories/data layer.
- Не смешивай UI-логику с domain-логикой.
- Единственный пользовательский интерфейс проекта — локальная web-app `mpstats_app/` + `web/`. Не добавляй новые CLI, notebook или отдельные desktop-GUI сценарии без явного запроса.
- Для web-app не заставляй пользователя редактировать сырой JSON/CSV, если можно дать нормальную форму. CRUD правил и справочников держи в services/data layer, routes оставляй тонкими.

## Ноутбуки и отчёты

- В аналитических и research-ноутбуках делай полезные визуализации прямо в notebook, где это возможно: графики распределений, качества, ошибок, bucket-разборов и динамики должны быть видны inline, а не только экспортироваться в CSV.
- Не ограничивайся таблицами, если график быстрее показывает вывод или проблему. CSV/артефакты нужны для воспроизводимости, но notebook должен быть читаемым визуально.

## Источники правды

- Основной пользовательский workflow: локальная web-app `mpstats_app/` + `web/`.
- Пользователь работает с данными на другом компьютере; эта машина используется для разработки. Локальные рабочие CSV/DB/override-файлы здесь не считай доказательством состояния пользовательского workflow без явного подтверждения.
- Индекс для агентов: `docs/AI_INDEX.md`.
- Навигация для агентов: `docs/AGENT_NAVIGATION.md`.
- Архитектурный контекст и исследовательское направление: `docs/ARCHITECTURE.md`.
- Человеческий вход в проект: `README.md`.
- Пользовательская инструкция: `docs/USER_GUIDE.md`.
- Справочник категорий: `Справочник категорий MP STATS.csv`.
- Настройки web workflow: `mpstats_app/api/settings.py`, `mpstats_app/services/project_service.py`, `pipeline/step1_config.py`; локальный `pipeline/step1_export_config.json` игнорируется, потому что в нём может быть cookie.
- Правила классификации: `classifiers/rules.csv`; основной редактор теперь вкладка `Классификатор` в локальной web-app.
- Локальное приложение: `mpstats_app/` + `web/`; справочник и правила редактируются через web UI и сохраняются обратно в CSV.

## Изменения

- Делай минимальный локальный diff.
- Не трогай unrelated flows.
- Не меняй analytics/event names или имена выходных колонок без явного требования.
- Если меняется schema, SQL, repository или model, опиши DB impact и добавь/обнови миграцию и тест.
- Не предлагай заменять production-БД локальной.
- При работе с `TASKS` копируй значения из справочника категорий буквально: пробелы, регистр, фильтры и path не нормализуй без запроса.
- Для справочника категорий используй только CSV `Справочник категорий MP STATS.csv`; Excel-дубликаты не считай источником правды.
- Синтаксис фильтра справочника держи в контракте `filterModel.name`: одиночный `contains`, `notContains` или комбинированная модель с `filterType`, `operator` (`AND`/`OR`), `condition1`, `condition2`. Пользовательское описание фильтров находится в `docs/USER_GUIDE.md`.
- Legacy-архив `TASKS` больше не хранится корневым Markdown-файлом. Если нужен технический слепок задач, клади его в ignored-зону `artifacts/task_archives/`, не в активную документацию.
- При любой правке, которая меняет пользовательский сценарий, интерфейс, расчёты, статусы, структуру результатов, справочник, классификатор или workflow, обновляй `docs/USER_GUIDE.md` в том же изменении.

## Проверки

- Запускай самый узкий релевантный тест или проверку.
- Для классификатора проверяй импорт/compile модулей `classifiers`.
- Для конфигов шага 1 проверяй импорт/compile модулей `pipeline`.
- Если тесты не запускались, честно напиши почему и что запустить вручную.

## Финальный ответ

Всегда укажи:

- изменённые файлы;
- DB impact;
- запущенные проверки;
- что проверить вручную.
